#ifndef SAME_LETTERS_HPP 
#define SAME_LETTERS_HPP 

#include <string>

// given two strings, return true if and only if they contain 
// exactly the same letters including repetition.
bool sameLetters(const std::string&, const std::string&);

#endif    // SAME_LETTERS_HPP 
